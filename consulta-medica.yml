spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5435/consulta_medica
    password: password
  jpa:
    database: postgresql
    show-sql: 'false'
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      uri: mongodb://localhost:28017/consultaMedica
